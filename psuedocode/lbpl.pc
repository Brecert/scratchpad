_not_ « not
_not_ » not

_print_ « print("Hello")
_not_ » _print_

@nodes « {} : Hash(Symbol, Node)

def traverse:
	for node in @nodes:
		for id, port in node.connections:
			@nodes[id][port].update(not.output)

def update:
	for node in @nodes:
		node.update()

